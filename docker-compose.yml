version: '3'

services:
  web:
    build: .
    #restart: unless-stopped
    networks:
      - ushahidiplatform_default
    ports:
      - "8080:8080"
    env_file: .env

networks:
  ushahidiplatform_default:
    external: true

